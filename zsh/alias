# Setup software
eval $(thefuck --alias)
eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
alias bat=batcat

# Setup utilities
alias update="sudo apt-get update; sudo apt-get upgrade -y; sudo apt-get dist-upgrade -y; sudo apt-get autoremove; sudo apt-get autoclean; sudo snap refresh; brew upgrade; npm update"
alias my_ip="neofetch public_ip; neofetch local_ip"
alias restart="sudo reboot"
alias fucking=sudo
alias please=sudo
alias show=tycat

alias git_gud="~/dotfiles/git/git_gud.sh"

# Setup small programs
alias bon_matin="source ~/.zshrc"

